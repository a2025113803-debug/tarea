#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, m;
    cin >> n;
    vector<double> A(n);

    for (int i = 0; i < n; i++) cin >> A[i];

    cin >> m;
    vector<double> B(m);

    for (int i = 0; i < m; i++) cin >> B[i];

    vector<double> C = A;
    for (double x : B) C.push_back(x);

    for (double x : C) cout << x << " ";
}

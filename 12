#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<double> v(n), r;

    for (int i = 0; i < n; i++) cin >> v[i];

    for (int i = 0; i < n; i++) {
        bool rep = false;

        for (int j = 0; j < r.size(); j++) {
            if (v[i] == r[j]) {
                rep = true;
                break;
            }
        }

        if (!rep) r.push_back(v[i]);
    }

    for (double x : r) cout << x << " ";
}

#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<double> v(n);

    for (int i = 0; i < n; i++) cin >> v[i];

    double suma = 0;
    for (double x : v) suma += x;

    cout << "Promedio = " << suma / n;
}

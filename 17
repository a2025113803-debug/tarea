#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<long long> F;

    if (n >= 1) F.push_back(0);
    if (n >= 2) F.push_back(1);

    for (int i = 2; i < n; i++) {
        F.push_back(F[i-1] + F[i-2]);
    }

    for (long long x : F) cout << x << " ";
}

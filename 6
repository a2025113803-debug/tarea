#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<double> v(n);

    for (int i = 0; i < n; i++) cin >> v[i];

    for (int i = 0; i < n - 2; i++) {
        double p = (v[i] + v[i+1] + v[i+2]) / 3.0;
        cout << p << "\n";
    }
}
